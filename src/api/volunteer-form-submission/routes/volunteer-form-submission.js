'use strict';

/**
 * volunteer-form-submission router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::volunteer-form-submission.volunteer-form-submission');
