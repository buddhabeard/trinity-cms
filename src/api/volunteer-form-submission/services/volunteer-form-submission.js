'use strict';

/**
 * volunteer-form-submission service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::volunteer-form-submission.volunteer-form-submission');
